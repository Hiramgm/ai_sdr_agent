# The ingestion pipeline uses the standard library only.

# Database persistence (PostgreSQL + MongoDB).
psycopg[binary]>=3.1
pymongo>=4.6
dnspython>=2.6
python-dotenv>=1.0

# Vector memory (Pinecone integrated embeddings).
pinecone
typing_extensions

# LLM decisions (outreach writing + review via Groq).
groq

# Planned additions, for example:
# fastapi
# uvicorn
# langgraph
# langchain-core
# redis
# rq
# playwright
# beautifulsoup4
# ragas
# langfuse
